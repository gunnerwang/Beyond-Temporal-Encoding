# What the method needs with its default prior. The LimiX checkout described in
# docs/data.md §3 brings its own dependencies, including huggingface_hub for the
# weight download.
#
# The alternative priors in plugin/model/lib/slow_priors.py import their backends
# lazily, so these are only needed if a configuration selects one:
#   tabpfn, tabpfn_extensions   TabPFNSlowPrior
#   transformers, sentence_transformers   LanguageModelSlowPrior
numpy>=1.24,<2.0
scipy>=1.10
scikit-learn>=1.3
torch>=2.0
pandas>=2.0
xgboost>=2.0
catboost>=1.2
lightgbm>=4.0
optuna>=3.4
tqdm>=4.65
